% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/testSilho.R
\name{silho.build.test}
\alias{silho.build.test}
\title{Build and Test Hybrid Partitions with Silhouette}
\usage{
silho.build.test(parameters)
}
\arguments{
\item{parameters}{A list containing:
\itemize{
\item \code{Number.Folds} (integer): Number of validation folds.
\item \code{Folders$folderPartitions} (string): Path to partitions folder.
\item \code{Folders$folderCommunities} (string): Path to communities folder.
\item \code{Folders$folderTestSilho} (string): Path to silhouette test folder.
\item \code{Folders$folderCVTR} (string): Path to cross-validation train dataset.
\item \code{Folders$folderCVVL} (string): Path to cross-validation validation dataset.
\item \code{Folders$folderCVTS} (string): Path to cross-validation test dataset.
\item \code{Folders$folderUtils} (string): Path to utility scripts and tools.
\item \code{Dataset.Name} (string): Name of the dataset.
\item \code{Dataset.Info$AttStart} (integer): Start index of attributes in the dataset.
\item \code{Dataset.Info$AttEnd} (integer): End index of attributes in the dataset.
\item \code{Dataset.Info$LabelStart} (integer): Start index of label columns.
\item \code{Choosed$all.methods.choosed} (data.frame): Selected methods for processing.
\item \code{Choosed$all.partitions.choosed} (data.frame): Chosen partitions for each fold and method.
\item \code{best.silhouette$all.silhouette} (data.frame): Best silhouette partitions.
}}
}
\value{
None. The function generates output files and directories but does not return a value.
}
\description{
This function constructs and evaluates hybrid partitions using silhouette-based methods.
It processes multiple folds in parallel, converts datasets between CSV and ARFF formats,
and executes classification tasks using the CLUS framework.
}
\details{
The function performs the following:
\itemize{
\item Processes folds in parallel using \verb{\%dopar\%}.
\item Reads train, validation, and test datasets for each fold.
\item Identifies the best partitioning strategy using silhouette metrics.
\item Converts CSV files to ARFF format for CLUS processing.
\item Executes CLUS classification tasks and extracts predictions.
\item Saves processed datasets, predictions, and cleans up intermediate files.
}
}
\examples{
\dontrun{
parameters <- list(
  Number.Folds = 5,
  Folders = list(
    folderPartitions = "/path/to/partitions",
    folderCommunities = "/path/to/communities",
    folderTestSilho = "/path/to/test/silho",
    folderCVTR = "/path/to/cv/train",
    folderCVVL = "/path/to/cv/validation",
    folderCVTS = "/path/to/cv/test",
    folderUtils = "/path/to/utils"
  ),
  Dataset.Name = "my_dataset",
  Dataset.Info = list(
    AttStart = 1, AttEnd = 20, LabelStart = 21
  ),
  Choosed = list(
    all.methods.choosed = data.frame(),
    all.partitions.choosed = data.frame()
  ),
  best.silhouette = list(
    all.silhouette = data.frame()
  )
)
silho.build.test(parameters)
}

}
