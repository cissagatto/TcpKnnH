% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/testMaF1.R
\name{maf1.test.gather.predicts}
\alias{maf1.test.gather.predicts}
\title{Gather Predictions from Cross-Validation Partitions}
\usage{
maf1.test.gather.predicts(parameters)
}
\arguments{
\item{parameters}{A list containing configuration settings, including folder
paths, number of folds, and reporting details.}
}
\value{
The function does not return a value but generates and saves the
aggregated prediction files (\code{y_true.csv} and \code{y_predict.csv}) in the
appropriate test partition directories.
}
\description{
This function aggregates true labels and predicted values across multiple
cross-validation folds and hybrid partitions.
}
\details{
The function iterates over each cross-validation fold and:
\itemize{
\item Loads partition and community data.
\item Determines the best partitioning method and its corresponding groups.
\item Collects \code{y_true.csv} and \code{y_predict.csv} files from test groups.
\item Deletes temporary files after processing.
\item Saves aggregated results in the respective partition folders.
}
}
\examples{
\dontrun{
parameters <- list(
  Number.Folds = 5,
  Folders = list(
    folderPartitions = "~/data/Partitions",
    folderCommunities = "~/data/Communities",
    folderTestMaF1 = "~/data/Test-MaF1",
    folderReports = "~/data/Reports"
  )
)
maf1.test.gather.predicts(parameters)
}

}
