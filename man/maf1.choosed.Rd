% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validateMaF1.R
\name{maf1.choosed}
\alias{maf1.choosed}
\title{Select and Save the Best Partitions for Each Fold and KNN Sparsification}
\usage{
maf1.choosed(parameters)
}
\arguments{
\item{parameters}{A list containing the necessary configuration settings, including:
\itemize{
\item \code{Folders}: A list specifying folder paths, including:
\itemize{
\item \code{folderReports} – Path to the report folder.
\item \code{folderPartitions} – Path to the partition data folder.
\item \code{folderCommunities} – Path to the community data folder.
}
\item \code{Dataset.Info}: A list containing dataset metadata, including:
\itemize{
\item \code{Labels} – The number of labels in the dataset.
}
\item \code{Number.Folds}: The number of cross-validation folds.
}}
}
\value{
A list containing:
\itemize{
\item \code{all.partitions.choosed} – Data frame with all selected partitions.
\item \code{all.methods.choosed} – Data frame with all chosen methods.
\item \code{all.eb.partitions} – Data frame with \code{eb} partitions.
\item \code{all.fg.partitions} – Data frame with \code{fg} partitions.
\item \code{all.wt.partitions} – Data frame with \code{wt} partitions.
\item \code{sparcification} – Data frame with fold-wise method count.
\item \code{all.partitions} – Data frame with all partitions.
\item \code{all.hybrid.partitions} – Data frame with hybrid partitions.
}
}
\description{
This function processes different partitions across multiple folds and KNN sparsifications,
identifying and saving the chosen partitions for further analysis.
}
\details{
The function follows these steps:
\enumerate{
\item Generates partition groups based on the dataset labels.
\item Iterates over each fold, loading selected partitions from CSV files.
\item Reads and processes partition files for different methods (\code{eb}, \code{fg}, \code{wt}) in each KNN configuration.
\item Aggregates results and saves them into multiple CSV files.
\item Returns a list containing all processed partition data.
}
}
\examples{
params <- list(
  Folders = list(
    folderReports = "path/to/reports",
    folderPartitions = "path/to/partitions",
    folderCommunities = "path/to/communities"
  ),
  Dataset.Info = list(Labels = 10),
  Number.Folds = 5
)
result <- maf1.choosed(params)
}
