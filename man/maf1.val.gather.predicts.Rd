% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validateMaF1.R
\name{maf1.val.gather.predicts}
\alias{maf1.val.gather.predicts}
\title{Gather Predictions from Machine Learning Experiment Partitions}
\usage{
maf1.val.gather.predicts(parameters)
}
\arguments{
\item{parameters}{A list containing the following elements:
\itemize{
\item \code{Number.Folds} (integer): Number of cross-validation folds.
\item \code{id_part} (integer): Identifier for the partition being processed.
\item \code{FolderPartition} (string): Path to the partition directory.
\item \code{Choosed} (list): Contains information about selected hybrid partitions.
}}
}
\value{
No explicit return value. The function processes and merges predictions,
then saves them into the partition directory.
}
\description{
This function collects prediction results from different folds and groups
within a partitioned dataset. It processes the stored prediction files,
merges them, and removes intermediate files.
}
\details{
The function iterates through each fold and group in the dataset:
\itemize{
\item Reads the predicted (\code{y_predict.csv}) and actual (\code{y_true.csv}) values.
\item Merges the results into consolidated prediction files.
\item Deletes intermediate files to free up space.
\item Saves the final merged predictions for further evaluation.
}

Parallel execution using \code{foreach} enables efficient data gathering.
}
\examples{
\dontrun{
parameters <- list(
  Number.Folds = 5,
  id_part = 2,
  FolderPartition = "/path/to/partition",
  Choosed = list(
    all.hybrid.partitions = data.frame(partitions = c(1, 2), groups = c(2, 3))
  )
)
maf1.val.gather.predicts(parameters)
}

}
