% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/testMaF1.R
\name{maf1.test.evaluate}
\alias{maf1.test.evaluate}
\title{Evaluate Multi-Label Classification Performance}
\usage{
maf1.test.evaluate(parameters)
}
\arguments{
\item{parameters}{A list containing configuration settings, including folder
paths, number of folds, and reporting details.}
}
\value{
The function does not return a value but generates evaluation results
and saves confusion matrices in the respective test partition directories.
}
\description{
This function computes performance metrics for multi-label classification
using confusion matrices across multiple cross-validation folds and KNN
partitions.
}
\details{
The function performs the following steps:
\itemize{
\item Iterates through each cross-validation fold.
\item Loads ground truth (\code{y_true.csv}) and predicted labels (\code{y_predict.csv}).
\item Computes confusion matrices and evaluation metrics.
\item Saves the results in a structured format.
\item Deletes temporary files after processing.
}
}
\examples{
\dontrun{
parameters <- list(
  Number.Folds = 5,
  Folders = list(
    folderPartitions = "~/data/Partitions",
    folderCommunities = "~/data/Communities",
    folderTestMaF1 = "~/data/Test-MaF1",
    folderReports = "~/data/Reports"
  )
)
maf1.test.evaluate(parameters)
}

}
