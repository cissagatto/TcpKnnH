% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validateSilho.R
\name{silhouete}
\alias{silhouete}
\title{Perform Silhouette Analysis for kNN-Based Multilabel Partitioning}
\usage{
silhouete(parameters)
}
\arguments{
\item{parameters}{A list containing the following elements:
\itemize{
\item \code{Number.Folds} (integer): Number of folds for cross-validation.
\item \code{Folders$folderValidation} (string): Path to validation folder.
\item \code{Folders$folderCommunities} (string): Path to communities folder.
\item \code{Folders$folderPartitions} (string): Path to partitions folder.
\item \code{LabelSpace$Classes} (list): List of label space classes.
\item \code{Dataset.Info$Labels} (integer): Number of labels in the dataset.
}}
}
\value{
No explicit return value. The function generates CSV reports containing:
\itemize{
\item Silhouette scores for each partition.
\item Best partition per fold based on silhouette values.
}
}
\description{
This function computes the silhouette scores for different kNN-based partitions
to evaluate clustering quality in a multilabel setting.
}
\details{
The function follows these steps:
\itemize{
\item Iterates through cross-validation folds.
\item Reads dataset label information and partitions.
\item Iterates through different kNN configurations, extracting partition data.
\item Computes silhouette scores:
\itemize{
\item If a partition has only one group, assigns NA.
\item If multiple groups exist, computes silhouette values and saves plots.
}
\item Stores results for each partition and selects the best one.
\item Saves CSV reports for analysis.
}
}
\examples{
\dontrun{
parameters <- list(
  Number.Folds = 5,
  Folders = list(
    folderValidation = "/path/to/validation",
    folderCommunities = "/path/to/communities",
    folderPartitions = "/path/to/partitions"
  ),
  LabelSpace = list(Classes = list(c("A", "B", "C"))),
  Dataset.Info = list(Labels = 3)
)
silhouete(parameters)
}

}
