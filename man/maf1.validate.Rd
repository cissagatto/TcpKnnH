% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validateMaF1.R
\name{maf1.validate}
\alias{maf1.validate}
\title{Perform Multilabel kNN Validation with Multiple Partitions}
\usage{
maf1.validate(parameters)
}
\arguments{
\item{parameters}{A list containing the following elements:
\itemize{
\item \code{Folders$folderReports} (string): Path to the reports directory.
\item \code{Folders$folderNamesLabels} (string): Path to the names and labels directory.
\item \code{Folders$folderValMaF1} (string): Path to the validation folder.
\item \code{Dataset.Name} (string): Name of the dataset being validated.
\item \code{Dataset.Info$Labels} (integer): Number of label partitions.
}}
}
\value{
No explicit return value. The function performs the validation process
and saves multiple evaluation results and runtime data as CSV files.
}
\description{
This function executes the validation process for a multilabel kNN-based
model, iterating through different partitions and performing evaluations.
}
\details{
The function follows these steps:
\itemize{
\item Reads dataset-specific configurations and labels.
\item Iterates through different k values for kNN.
\item Iterates through label partitions, executing the following validation steps:
\enumerate{
\item Calls \code{maf1.validate.partitions()} to validate partitions.
\item Calls \code{maf1.val.gather.predicts()} to collect predictions.
\item Calls \code{maf1.val.evaluate()} to compute evaluation metrics.
\item Calls \code{maf1.val.gather.evaluated()} to aggregate evaluation results.
\item Saves runtime statistics for each step.
}
\item Deletes temporary files and manages memory usage.
}
}
\examples{
\dontrun{
parameters <- list(
  Folders = list(
    folderReports = "/path/to/reports",
    folderNamesLabels = "/path/to/nameslabels",
    folderValMaF1 = "/path/to/validation"
  ),
  Dataset.Name = "example_dataset",
  Dataset.Info = list(Labels = 5)
)
maf1.validate(parameters)
}

}
