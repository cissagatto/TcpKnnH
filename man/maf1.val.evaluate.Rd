% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validateMaF1.R
\name{maf1.val.evaluate}
\alias{maf1.val.evaluate}
\title{Evaluate Multilabel Classification Performance}
\usage{
maf1.val.evaluate(parameters)
}
\arguments{
\item{parameters}{A list containing the following elements:
\itemize{
\item \code{Number.Folds} (integer): Number of cross-validation folds.
\item \code{FolderPartition} (string): Path to the partition directory.
}}
}
\value{
No explicit return value. The function saves evaluation metrics
and confusion matrices in the corresponding directories.
}
\description{
This function evaluates the performance of a multilabel classification
experiment by computing confusion matrices and evaluation metrics for
each fold in a partitioned dataset.
}
\details{
The function iterates through each fold:
\itemize{
\item Reads the ground-truth (\code{y_true.csv}) and predicted (\code{y_predict.csv}) labels.
\item Converts the data for multilabel evaluation.
\item Computes the multilabel confusion matrix and saves it.
\item Evaluates multilabel classification metrics and stores results.
\item Deletes intermediate prediction files after evaluation.
}

Parallel execution using \code{foreach} optimizes computation.
}
\examples{
\dontrun{
parameters <- list(
  Number.Folds = 5,
  FolderPartition = "/path/to/partition"
)
maf1.val.evaluate(parameters)
}

}
